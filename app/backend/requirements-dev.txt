# Dev-only dependencies for the DynaChat backend.
#
# Install alongside requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# The Dark Factory `dark-factory-fix-issue` command installs these
# automatically inside the worktree venv before running validation.

# Lint + format (one tool).
ruff==0.9.2

# Type checker.
mypy==1.14.1

# Test runner + helpers for async FastAPI routes.
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
